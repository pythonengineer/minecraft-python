[build-system]
requires = [
    "setuptools",
    "wheel",
    "cython>=3.0.8",
    "numpy>=1.26.0",
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
# PyPy 3.10 doesn't get along with NumPy for some reason
skip = "pp310*"
